filters: ["diagram/diagram.lua"]

metadata:
  # Diagram filter config
  diagram:
    engine:
      tikz:
        additional-packages: |
          \usepackage{adjustbox}
          \usetikzlibrary{arrows, shapes, automata}
