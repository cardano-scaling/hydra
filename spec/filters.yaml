filters: ["diagram.lua"]

metadata:
  # Diagram filter config
  diagram:
    engine:
      mermaid:
        mime-type:
          application/pdf: true
      tikz:
        additional-packages: |
          \usepackage{adjustbox}
          \usetikzlibrary{arrows, shapes, automata}
