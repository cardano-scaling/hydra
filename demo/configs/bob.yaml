# Demo config for bob (signing peer 2 of 3). See alice.yaml for how the
# shared "peers:" block works.
node-id: "2"
listen: "127.0.0.1:5002"
advertise: "127.0.0.1:5002"
api-port: 4002
monitoring-port: 6002
hydra-signing-key: "../bob.sk"
peers:
  - address: "127.0.0.1:5001"
    hydra-verification-key: "../alice.vk"
    fuel-verification-key: "../../hydra-cluster/config/credentials/alice.vk"
  - address: "127.0.0.1:5002" # self — keys here are ignored (filtered out)
    hydra-verification-key: "../bob.vk"
    fuel-verification-key: "../../hydra-cluster/config/credentials/bob.vk"
  - address: "127.0.0.1:5003"
    hydra-verification-key: "../carol.vk"
    fuel-verification-key: "../../hydra-cluster/config/credentials/carol.vk"
  - address: "127.0.0.1:5004" # alice-mirror: observer peer (no keys)
ledger-protocol-parameters: "../../devnet/protocol-parameters.json"
persistence-dir: "../../devnet/persistence/bob"
chain:
  mode: cardano
  fuel-signing-key: "../../hydra-cluster/config/credentials/bob.sk"
  contestation-period: 3
  deposit-period: 10
  deposit-activation: 10
  backend:
    mode: direct
    testnet-magic: 42
    node-socket: "../../devnet/node.socket"
