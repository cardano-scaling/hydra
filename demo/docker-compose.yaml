services:
  cardano-node:
    image: ghcr.io/intersectmbo/cardano-node:10.4.1
    volumes:
      - ./devnet:/devnet
    environment:
      - CARDANO_BLOCK_PRODUCER=true
      - CARDANO_SOCKET_PATH=/devnet/node.socket # used by cardano-node
      - CARDANO_NODE_SOCKET_PATH=/devnet/node.socket # used by cardano-cli
    command:
      [ "run"
      , "--config", "/devnet/cardano-node.json"
      , "--topology", "/devnet/topology.json"
      , "--database-path", "/devnet/db"
      , "--shelley-kes-key", "/devnet/kes.skey"
      , "--shelley-vrf-key", "/devnet/vrf.skey"
      , "--shelley-operational-certificate", "/devnet/opcert.cert"
      , "--byron-delegation-certificate", "/devnet/byron-delegation.cert"
      , "--byron-signing-key", "/devnet/byron-delegate.key"
      ]

  hydra-setup:
    # TODO: implement
    image: ghcr.io/cardano-scaling/hydra-setup:latest
    environment:
      - ACTORS="alice,bob,carol"
      - CARDANO_NODE_SOCKET_PATH=/devnet/node.socket
      - CARDANO_NODE_NETWORK_ID=42
      - SEED_NETWORK=true
    volumes:
      - ./setup:/setup
      - ./devnet:/devnet
    depends_on:
      - cardano-node

  hydra-node-1:
   # NOTE: Make sure to use the same image in ./seed-devnet.sh
    image: ghcr.io/cardano-scaling/hydra-node:0.22.4
    build:
      context: ../
      target: hydra-node
    depends_on:
      hydra-setup:
        condition: service_completed_successfully
    volumes:
      - ./setup:/setup:ro
      - ./devnet:/devnet
    ports:
      - "4001:4001"
      - "5001:5001"
    command:
      [ "--node-id", "1"
      , "--api-host", "0.0.0.0"
      , "--listen", "172.16.238.10:5001"
      , "--monitoring-port", "6001"
      , "--peer", "172.16.238.20:5001"
      , "--peer", "172.16.238.30:5001"
      , "--hydra-scripts-tx-id", "/setup/hydra-scripts.txid"
      , "--hydra-signing-key", "/setup/setup/keys/alice-hydra-hydra.sk"
      , "--hydra-verification-key", "/setup/keys/bob-hydra.vk"
      , "--hydra-verification-key", "/setup/setup/keys/carol-hydra-hydra.vk"
      , "--cardano-signing-key", "/setup/setup/keys/alice-hydra-cardano.sk"
      , "--cardano-verification-key", "/setup/keys/bob-cardano.vk"
      , "--cardano-verification-key", "/setup/keys/carol-hydra-cardano.vk"
      , "--ledger-protocol-parameters", "/setup/protocol-parameters.json"
      , "--testnet-magic", "42"
      , "--node-socket", "/devnet/node.socket"
      , "--persistence-dir", "/devnet/persistence/alice"
      , "--contestation-period", "3s"
      ]
    networks:
      hydra_net:
        ipv4_address: 172.16.238.10
    restart: always

  hydra-node-2:
    # NOTE: Make sure to use the same image in ./seed-devnet.sh
    image: ghcr.io/cardano-scaling/hydra-node:0.22.4
    build:
      context: ../
      target: hydra-node
    depends_on:
      hydra-setup:
        condition: service_completed_successfully
    volumes:
      - .:/keys:ro
      - ./devnet:/devnet
    ports:
      - "4002:4001"
      - "5002:5001"
    command:
      [ "--node-id", "2"
      , "--api-host", "0.0.0.0"
      , "--listen", "172.16.238.20:5001"
      , "--monitoring-port", "6001"
      , "--peer", "172.16.238.10:5001"
      , "--peer", "172.16.238.30:5001"
      , "--hydra-scripts-tx-id", "/setup/hydra-scripts.txid"
      , "--hydra-signing-key", "/setup/keys/bob-hydra.sk"
      , "--hydra-verification-key", "/setup/keys/alice-hydra.vk"
      , "--hydra-verification-key", "/setup/keys/carol-hydra.vk"
      , "--cardano-signing-key", "/setup/keys/bob-cardano.sk"
      , "--cardano-verification-key", "/setup/keys/alice-cardano.vk"
      , "--cardano-verification-key", "/setup/keys/carol-hydra-cardano.vk"
      , "--ledger-protocol-parameters", "/setup/protocol-parameters.json"
      , "--testnet-magic", "42"
      , "--node-socket", "/devnet/node.socket"
      , "--persistence-dir", "/devnet/persistence/bob"
      , "--contestation-period", "3s"
      ]
    networks:
      hydra_net:
        ipv4_address: 172.16.238.20
    restart: always

  hydra-node-3:
    # NOTE: Make sure to use the same image in ./seed-devnet.sh
    image: ghcr.io/cardano-scaling/hydra-node:0.22.4
    build:
      context: ../
      target: hydra-node
    depends_on:
      hydra-setup:
        condition: service_completed_successfully
    volumes:
      - .:/keys:ro
      - ./devnet:/devnet
    ports:
      - "4003:4001"
      - "5003:5001"
    command:
      [ "--node-id", "3"
      , "--api-host", "0.0.0.0"
      , "--listen", "172.16.238.30:5001"
      , "--monitoring-port", "6001"
      , "--peer", "172.16.238.10:5001"
      , "--peer", "172.16.238.20:5001"
      , "--hydra-scripts-tx-id", "/setup/hydra-scripts.txid"
      , "--hydra-signing-key", "/setup/keys/carol-hydra.sk"
      , "--hydra-verification-key", "/setup/keys/alice-hydra.vk"
      , "--hydra-verification-key", "/setup/keys/bob-hydra.vk"
      , "--cardano-signing-key", "/setup/keys/carol-cardano.sk"
      , "--cardano-verification-key", "/setup/keys/alice-cardano.vk"
      , "--cardano-verification-key", "/setup/keys/bob-cardano.vk"
      , "--ledger-protocol-parameters", "/setup/protocol-parameters.json"
      , "--testnet-magic", "42"
      , "--node-socket", "/devnet/node.socket"
      , "--persistence-dir", "/devnet/persistence/carol"
      , "--contestation-period", "3s"
      ]
    networks:
      hydra_net:
        ipv4_address: 172.16.238.30
    restart: always

  hydra-tui-1:
    image: ghcr.io/cardano-scaling/hydra-tui:0.22.4
    build:
       context: ../
       target: hydra-tui
    depends_on:
      - hydra-node-1
    command:
      [ "--connect", "172.16.238.10:4001"
      , "--node-socket", "/devnet/node.socket"
      , "--testnet-magic", "42"
      , "--cardano-signing-key", "/setup/keys/alice-cardano-funds.sk"
      ]
    volumes:
      - ./setup:/setup:ro
      - ./devnet:/devnet
    networks:
      hydra_net:
        ipv4_address: 172.16.238.11

  hydra-tui-2:
    image: ghcr.io/cardano-scaling/hydra-tui:0.22.4
    build:
       context: ../
       target: hydra-tui
    depends_on:
      - hydra-node-2
    command:
      [ "--connect", "172.16.238.20:4001"
      , "--node-socket", "/devnet/node.socket"
      , "--testnet-magic", "42"
      , "--cardano-signing-key", "/setup/keys/bob-cardano-funds.sk"
      ]
    volumes:
      - ./setup:/setup:ro
      - ./devnet:/devnet
    networks:
      hydra_net:
        ipv4_address: 172.16.238.21

  hydra-tui-3:
    image: ghcr.io/cardano-scaling/hydra-tui:0.22.4
    build:
       context: ../
       target: hydra-tui
    depends_on:
      - hydra-node-3
    command:
      [ "--connect", "172.16.238.30:4001"
      , "--node-socket", "/devnet/node.socket"
      , "--testnet-magic", "42"
      , "--cardano-signing-key", "/setup/keys/carol-hydra-cardano-funds.sk"
      ]
    volumes:
      - ./setup:/setup:ro
      - ./devnet:/devnet
    networks:
      hydra_net:
        ipv4_address: 172.16.238.31

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    command:
    - --config.file=/etc/prometheus/prometheus.yml
    volumes:
    - ./prometheus/docker/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    networks:
      hydra_net:
        ipv4_address: 172.16.238.5

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
      - DS_PROMETHEUS=prometheus
    volumes:
      - "./grafana/docker/datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml"
      - "./grafana/docker/dashboard.json:/var/lib/grafana/dashboards/dashboard.json"
      - "./grafana/docker/default.yaml:/etc/grafana/provisioning/dashboards/default.yaml"
    networks:
      hydra_net:
        ipv4_address: 172.16.238.8

networks:
  hydra_net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.16.238.0/24
          gateway: 172.16.238.1
