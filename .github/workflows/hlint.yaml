name: Check hlint

on:
  pull_request:

jobs:
  hlint:
    name: Check hlint
    runs-on: self-hosted
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4

    - name: 📐 Check hlint
      run: |
        nix develop .#fmt --command hlint .
