name: "Compute cost differences"

on:
  # Kept so the merge queue still sees this check, even though the job itself
  # only runs for pull_request events.
  merge_group:
  pull_request:

permissions:
  checks: write
  pull-requests: write


jobs:
  # Compute the cost difference between this branch and master.
  tx-cost-diff:
    name: Compute cost differences
    runs-on: ubuntu-latest
    # Only run on PR
    if: github.event_name == 'pull_request'
    steps:
    - name: "Checkout the PR as the 'new' source"
      uses: actions/checkout@v4

    - name: ❄ Setup Nix/Cachix
      uses: ./.github/actions/nix-cachix-setup
      with:
        authToken: '${{ secrets.CACHIX_CARDANO_SCALING_AUTH_TOKEN }}'

    - name: "Compute the difference markdown"
      run: |
        nix run ".#tx-cost-diff"

    - name: 💾 Upload tx cost diff artifact
      uses: actions/upload-artifact@v4
      with:
        name: tx-cost-diff
        path: diff.md
        if-no-files-found: error

    - name: 🔎 Find Comment
      if: github.event.pull_request.head.repo.full_name == github.repository
      uses: peter-evans/find-comment@v3
      id: find-comment
      with:
        issue-number: ${{ github.event.pull_request.number }}
        comment-author: 'github-actions[bot]'
        body-includes: Transaction cost differences

    - name: ✏ Create or update comment
      if: github.event.pull_request.head.repo.full_name == github.repository
      uses: peter-evans/create-or-update-comment@v4
      with:
        comment-id: ${{ steps.find-comment.outputs.comment-id }}
        edit-mode: replace
        issue-number: ${{ github.event.pull_request.number }}
        body-file: diff.md
