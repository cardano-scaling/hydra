# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader
/static/docs-metadata.json
/static/hydra-spec.pdf

# Generated fresh on every docs build from the current code (see nix/hydra/docs.nix
# preBuild and the `docs` just recipe). Never committed.
/benchmarks/transaction-cost.md

# plantuml byproducts from `yarn regenerate-plantuml`. Only the committed
# hydra-components.svg is used; the .png and .cmapx regenerate on every build.
/docs/dev/architecture/hydra-components.png
/docs/dev/architecture/hydra-components.cmapx

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.log/
